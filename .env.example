# ─── Logging ────────────────────────────────────────────────────────────────
LOG_LEVEL=info
LOG_CONSOLE=true
LOG_TRANSPORT=file
LOG_ROTATION=hourly

# ─── Tracing (fastrace) ─────────────────────────────────────────────────────
# Enable span collection. Default: false (max performance).
TRACING_ENABLE=false

# Sampling rate (0.0–1.0). Only applies when TRACING_ENABLE=true.
# 1.0 = trace every request, 0.5 = trace half, 0.0 = trace none.
TRACING_SAMPLING=0.7

# Per-request span reporter. Set to "console" to emit spans to stderr for
# debugging. When unset, spans are collected but not output.
TRACING_REPORTER=console
